# PLG Commit 0004F.1 — Vendor Quotes UI Fix

This fixes the prior installer validation and applies the same intended UI update more robustly.

## Changes

- Uses **Vendor Quotes** terminology
- Makes Vendor Quote sections expandable and collapsible
- Enlarges Vendor Quote text
- Matches typography to the rest of PLG
- Preserves imports, quantity editing, Basket selection, checkout, and pricing

## Install

Stop PLG, then run:

```bash
python3 install_commit_0004F_1.py
```

Restart PLG and open the same job.
