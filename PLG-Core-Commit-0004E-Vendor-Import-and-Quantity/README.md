# PLG Commit 0004E — Vendor Import & Editable Quantity

- Fixes Firefox extension import endpoint
- Returns to Parts Basket after import
- Keeps the single Vendor dropdown
- Adds editable quantity in the final Parts Basket

Install:
```bash
python3 install_commit_0004E.py
```

Then restart PLG and reload the Firefox extension from:
`about:debugging#/runtime/this-firefox`
