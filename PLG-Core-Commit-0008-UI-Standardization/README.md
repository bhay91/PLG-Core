# PLG Commit 0008 — UI Standardization

This update:

- Removes overlapping temporary CSS patches from commits 0007A/0007B
- Adds one shared management toolbar standard
- Aligns search fields and filter buttons
- Standardizes filter button height and width
- Standardizes rows, tables, and action groups
- Keeps New buttons visible
- Includes responsive layouts
- Creates a backup before changing `static/app.css`

## Install

```bash
cd ~/Desktop/PLG-Core-Commit-0008-UI-Standardization
python3 install_commit_0008.py
```

Then hard-refresh Firefox with `Ctrl + Shift + R`.
