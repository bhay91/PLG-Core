# PLG Commit 0004G — Checkout-to-Quote Bridge

Fixes the error:

`Add at least one part before generating a quote.`

## Install

Stop PLG, then run:

```bash
python3 install_commit_0004G.py
```

Restart PLG, reopen the job, click **Checkout Basket**, then click **Create Quote**.
